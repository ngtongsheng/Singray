# Force LF everywhere regardless of platform/runner autocrlf settings.
# Windows runners default to core.autocrlf=true, which converts checked-out
# files to CRLF and breaks biome's format check (lineEnding: lf) on every
# tracked text file.
* text=auto eol=lf
