# Raw feedback — 2026-06-14 (Round 2 source)

Verbatim user dump. Triaged into Round 2 stories in `BACKLOG.md`. Do not edit; this is the source record.

## Edit lyric
- Bug: disable stamp tab key when preview
- Bug: some sentence do no have all character tabbed but it shown as completed line. can we indicate partial completed?
- the label 全部打完 - 可以预览了 is too prominent position in the middle. can we do something? maybe use the same progress bar like the one when downloaded and splitting?
- can we use tab for switch between add lyric/tab lyric/preview
- When click back button from edit lyric should go back to song page

## Navigation bar
- Drop unified header, make header 2 rows. Top row is the app logo (left) and window controls minimize, fullscreen and close (right)
- Page level control will place below app header
- Let's try backgroundless design for nav (or using transparent gradient) like the control
- Arranage title and artist vertically

## Control
- Make dropdown component nicer looking rather than the native look
- Don't use native scrollbar ui
- Able to close dialog and popup when click outside
- generally avoid native looking ui

## Card
- Hide number of sung count.

## Home
- Add list view

## Add song
- Can we use tab to navigate between youtube by search + url and upload from file ?
- Make ui load from file draggable
- Auto detect language as well based on the available language added by user.

## Song
- Add a way to view song details (title, sung count, artist etc), perhaps a modal pop up?
- Can we move lyric behind the player control? the bottom part of scrolling lyric is currently covering the waveform and controll
- Can we animate the background when singing, right now it is just static blur. subtle movement would be cool

## Lyric ai clean up
- Can we should diff like how we show code diff?
- Bug: ai deleted the song original lyric, it should only delete the no lyric words like [bridge] [artist name] etc

## New page
- By artist view
- Click on artist name will link to artist view

## Edit meta data
- Align ai clean up button with cancel and save
